# Try Apollo with MongoDB

Forked from [arunoda/try-apollo](https://github.com/arunoda/try-apollo)

#### Using Apollo + GraphQL + MongoDB + Express

To get setup:

clone repository

#### Server
`yarn && yarn start`
#### UI
`yarn && yarn start`
